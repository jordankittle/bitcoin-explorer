const Error = () => {
    return (
        <div className="wrap error">
            <h2>Error</h2>
            <p>Sorry! We just encountered an unexpected error.</p>
        </div>
    );
};

export default Error;